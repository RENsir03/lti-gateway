<?php

/**
 * LTI Gateway 配置文件
 */

return [

    /*
    |--------------------------------------------------------------------------
    | 默认虚拟邮箱域名
    |--------------------------------------------------------------------------
    */
    'virtual_email_domain' => env('DEFAULT_VIRTUAL_EMAIL_DOMAIN', 'proxy.university.edu'),

    /*
    |--------------------------------------------------------------------------
    | Token 过期时间 (秒)
    |--------------------------------------------------------------------------
    */
    'token_ttl' => env('LTI_TOKEN_TTL', 600),
    'max_launch_age' => env('LTI_MAX_LAUNCH_AGE', 3600),

    /*
    |--------------------------------------------------------------------------
    | 下游 Moodle 配置
    |--------------------------------------------------------------------------
    */
    'moodle' => [
        'base_url' => env('MOODLE_BASE_URL', 'http://moodle:8080'),
        'timeout' => env('MOODLE_API_TIMEOUT', 30),
        'max_retry' => env('MOODLE_MAX_RETRY', 3),
        'webservice_token' => env('MOODLE_WEBSERVICE_TOKEN'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 日志配置
    |--------------------------------------------------------------------------
    */
    'logging' => [
        'retention_days' => env('LAUNCH_LOG_RETENTION_DAYS', 90),
    ],

    /*
    |--------------------------------------------------------------------------
    | 安全配置
    |--------------------------------------------------------------------------
    */
    'security' => [
        'rate_limit' => env('RATE_LIMIT_PER_MINUTE', 60),
        'nonce_ttl' => 86400, // 24小时
        'max_failed_attempts' => 5,
        'lockout_duration' => 300, // 5分钟
    ],

    /*
    |--------------------------------------------------------------------------
    | 健康检查配置
    |--------------------------------------------------------------------------
    */
    'health_check' => [
        'enabled' => env('HEALTH_CHECK_ENABLED', true),
        'interval' => env('HEALTH_CHECK_INTERVAL', 60),
    ],

];
